{
  "name": "routemind",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "cd project && vite",
    "build": "cd project && vite build",
    "preview": "cd project && vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.2"
  }
}